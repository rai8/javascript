/*const square = function(number) {
  return number * number;
};*/
//we can simplify this code using the arrow function and maki it more clean
const square = number => number * number;
console.log(square(5));
