let example = [5, 5, 6];
example.push(8, 9);
console.log(example);
//listing values... we use square brackets

let listNumbers = [2, 3, 5, 6, 7, 11]; // the first index of an array is zero, hence index 0f 2=0,3=1 and so on
console.log(listNumbers.pop());
console.log(listNumbers);
/*
types of properties 
toUpperCase, toLowerCase- converts string to uppercase or lowercase 
push- we use it to add values to your given array
pop- does the opposite of push, it removes last value to a give array
*/
