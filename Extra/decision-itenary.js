let a = 1,
  b = "1";
let result = a == b ? "equal" : "not equal";
console.log(result);
// the '?' states that if its true perfom the given operation
//the ':' states that if not true well perfom the next operation
