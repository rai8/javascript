setTimeout(function() {
  console.log("We waited for 5 seconds");
}, 5000);
// function expression is when we never want to use that function again, we apply when we want to use that function only once
